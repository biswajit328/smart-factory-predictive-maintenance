services:
  - type: web
    name: smart-factory-predictive-maintenance-api
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PM_API_HOST
        value: 0.0.0.0
      - key: PM_LOG_LEVEL
        value: INFO
